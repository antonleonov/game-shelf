-- Database initialization script
-- This runs automatically when the container is first created

-- Create database if it doesn't exist (postgres image does this automatically via POSTGRES_DB)
-- But we can add any custom initialization here

-- Grant permissions
GRANT ALL PRIVILEGES ON DATABASE gameshelf TO gameshelf;

